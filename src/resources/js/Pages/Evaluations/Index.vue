<script setup>
import AppShell from '../../Components/Ui/AppShell.vue'
const props = defineProps({ group: Object, periods: Array, students: Array })
</script>
<template>
<AppShell><template #toolbar><a :href="`/unterrichtsgruppen/${group.id}`" class="btn btn-sm btn-light" title="Schließen" aria-label="Schließen"><i class="bi bi-x-lg" aria-hidden="true"></i></a><a class="btn btn-sm btn-primary ms-2" :href="`/unterrichtsgruppen/${group.id}/bewertungen/neu`"><i class="bi bi-plus-lg me-1" aria-hidden="true"></i>Bewertungszeitraum anlegen</a></template><div class="container-full px-3 py-4"><h1 class="h2">Bewertungen · {{ group.name }}</h1><div v-if="$page.props.flash?.success" class="alert alert-success">{{ $page.props.flash.success }}</div><div v-if="!periods.length" class="card"><div class="card-body text-muted">Noch kein Bewertungszeitraum angelegt.</div></div><div v-for="period in periods" :key="period.id" class="card mb-3"><div class="card-body"><h2 class="h5">{{ period.label }}</h2><div v-for="evaluation in period.evaluations" :key="evaluation.id" class="border-top py-2 d-flex justify-content-between align-items-start"><div><strong>{{ evaluation.student.last_name }}, {{ evaluation.student.first_name }}</strong><span class="badge ms-2" :class="evaluation.status === 'confirmed' ? 'text-bg-success' : 'text-bg-light'">{{ evaluation.status === 'confirmed' ? 'bestätigt' : 'Entwurf' }}</span><p class="mb-0 mt-1 text-pre-wrap">{{ evaluation.draft_text || 'Noch kein Bewertungsentwurf.' }}</p></div><a class="btn btn-sm btn-outline-primary" :href="`/unterrichtsgruppen/${group.id}/bewertungen/${evaluation.id}/bearbeiten`">Bearbeiten</a></div></div></div></div></AppShell>
</template>
